const pets: Array<string> = ["gato", "rato", "pato"];

console.log(pets[0]);
console.log(pets[pets.length - 1]);

const txt: string = pets.join(", "); //adiciona as virgulas e imprime apenas o conteúdo dentro do []
console.log(txt);
